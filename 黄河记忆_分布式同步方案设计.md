# 赛题一《黄河记忆》分布式同步方案设计

> 目标：基于鸿蒙分布式数据服务，实现收藏、笔记、学习进度在手机/平板多设备间的自动同步，
> 并在多设备离线编辑冲突时「以最新时间戳版本为准」，提供同步状态可视化面板。

---

## 一、存储架构选型

| 方案 | 说明 | 结论 |
|------|------|------|
| A. 关系型库 RelationalStore 做本地 + KVStore 做同步通道 | 需维护双写一致性，复杂度高 | 不推荐 |
| **B. distributedKVStore 单库（推荐）** | 收藏/笔记/进度统一以 KV 存储，value 为 JSON 字符串；本地查询用内存过滤；同步由 KVStore 自动完成 | **首选** |
| C. 关系型库开启分布式表 | 结构化强，但冲突粒度偏设备级，难以做「记录级时间戳仲裁」 | 备选 |

**选择方案 B 的理由**：
1. 赛题核心是「分布式数据管理」，用 `distributedKVStore` 最直接地展示鸿蒙分布式能力；
2. 个人收藏/笔记数据量小（通常几十条），内存遍历即可满足查询，无需关系型数据库；
3. KVStore 天然支持离线编辑 + 网络恢复自动同步，与赛题要求完全吻合；
4. 记录级时间戳冲突策略可在应用层精确实现（见第四节）。

---

## 二、数据模型设计（实体定义）

所有需要跨设备同步的「可变数据」都带三个公共字段：`updatedAt`（冲突判定关键）、`deleted`（墓碑标记）、`id`（跨设备唯一标识）。

### 2.1 收藏记录 Favorite

```ts
export interface Favorite {
  /** 跨设备唯一标识（UUID，创建时生成后不再变化） */
  id: string;
  /** 关联的遗址 ID */
  heritageId: string;
  /** 收藏标签分类，如 ['待实地探访'] ['已去过'] ['研究资料'] */
  tags: string[];
  /** 创建时间（毫秒时间戳） */
  createdAt: number;
  /** 最后更新时间（毫秒时间戳），冲突判定的唯一依据 */
  updatedAt: number;
  /** 逻辑删除墓碑标记（true 表示已删除） */
  deleted: boolean;
}
```

### 2.2 笔记 Note

```ts
export interface Note {
  id: string;
  heritageId: string;
  content: string;
  createdAt: number;
  updatedAt: number;
  deleted: boolean;
}
```

### 2.3 学习进度 Progress

```ts
export interface Progress {
  /** 直接以遗址 ID 作为唯一标识（每个遗址只有一条进度） */
  heritageId: string;
  /** 最近一次浏览时间 */
  lastReadAt: number;
  /** 浏览次数 */
  readCount: number;
  updatedAt: number;
  deleted: boolean;
}
```

### 2.4 同步元数据 SyncMeta（用于可视化面板）

```ts
export interface SyncMeta {
  /** 最后同步时间 */
  lastSyncTime: number;
  /** 累计冲突记录数 */
  conflictCount: number;
  /** 当前设备标识 */
  deviceId: string;
}
```

---

## 三、KV 键设计

### 3.1 命名规范

统一格式：`类型前缀:唯一标识`

| 前缀 | 类型 | 键示例 | value 内容 |
|------|------|--------|-----------|
| `fav:` | 收藏 | `fav:8f14e45f-ea21-4c9b-8f41-9d5a...` | Favorite JSON |
| `note:` | 笔记 | `note:3b2c...` | Note JSON |
| `prog:` | 学习进度 | `prog:erlitou` | Progress JSON |
| `meta:` | 同步元数据 | `meta:syncState` | SyncMeta JSON |

### 3.2 设计要点

1. **UUID 生成后固定不变**：同一条记录在多设备间始终用同一个 key，KVStore 才能正确合并，这是「记录级冲突仲裁」的前提。
2. **删除用墓碑，不物理删除 key**：删除操作不调用 `kvStore.delete(key)`，而是 `put` 一条 `deleted: true` 且 `updatedAt` 最新的墓碑记录。这样多设备离线期间「A 删除 / B 修改」的冲突也能通过时间戳仲裁。
3. **`prog:` 用遗址 ID 作 key**：每个遗址的进度全局唯一，天然无需 UUID。

---

## 四、时间戳冲突处理算法（核心）

### 4.1 基本读写规则

```ts
// 新增 / 更新：写入时刷新 updatedAt
function putFavorite(fav: Favorite): void {
  fav.updatedAt = Date.now();
  fav.deleted = false;
  kvStore.put(`fav:${fav.id}`, JSON.stringify(fav));
}

// 删除：写墓碑
function deleteFavorite(id: string): void {
  const old = getFavorite(id);
  const tombstone: Favorite = {
    ...old,
    deleted: true,
    updatedAt: Date.now()   // 关键：墓碑时间戳更新，保证删除能"赢过"旧修改
  };
  kvStore.put(`fav:${id}`, JSON.stringify(tombstone));
}
```

### 4.2 冲突仲裁规则

**规则：同一条记录（同 key）多版本并存时，比较 `updatedAt`，最新者胜；若时间戳相同，再比较 `id` 保证确定性。**

由于 KVStore 同 key 合并时系统默认采用 LAST_WRITE_WINS（以设备时钟为准），可能与「应用层时间戳」不完全一致，因此**在 `dataChange` 回调中做二次仲裁**，确保最终一致性以 `updatedAt` 为准：

```ts
kvStore.on('dataChange', distributedKVStore.SubscribeType.SUBSCRIBE_TYPE_ALL,
  (data) => {
    // 遍历本机缓存的记录，与 KVStore 中最新值比对
    for (const key of Object.keys(localCache)) {
      const local = localCache[key];
      const remoteStr = await kvStore.get(key);   // 同步过来的最新值
      const remote = JSON.parse(remoteStr);
      // 以 updatedAt 最新者为准
      if (remote.updatedAt > local.updatedAt) {
        localCache[key] = remote;      // 采纳远端
        if (remote.updatedAt === local.updatedAt) {
          // 时间戳相同，用 id 兜底保证确定性
        }
      } else if (local.updatedAt > remote.updatedAt) {
        kvStore.put(key, JSON.stringify(local));  // 本机更新，写回纠正
        conflictCount++;
      }
      // 墓碑过滤：deleted 为 true 的记录不出现在 UI 列表
    }
  }
);
```

### 4.3 冲突计数与面板数据

- 每次「本机写回纠正」或「比较后发现不一致」时，`conflictCount++`，并更新 `meta:syncState`；
- 面板展示三项：`lastSyncTime`（最近同步时间）、`conflictCount`（冲突记录数）、当前设备 `deviceId`。

---

## 五、同步状态可视化面板

面板数据来源为 `meta:syncState`，UI 呈现：

```
┌─────────────────────────────────┐
│ 数据同步状态                      │
│  最后同步：2026-11-01 14:32       │
│  冲突记录：2 条                   │
│  当前设备：MatePad Pro            │
│  同步方向：手机 → 平板            │
└─────────────────────────────────┘
```

实现要点：
- 订阅 `dataChange` 后刷新面板；
- 用 `distributedKVStore` 的 `getResultSet` 或遍历 key 统计待同步/冲突数量；
- 进度条可用 `Progress` 组件模拟「同步进度」。

---

## 六、同步生命周期

```
用户新增收藏 / 编辑笔记 / 标记标签
        ↓
put 写入 KVStore（updatedAt = now）
        ↓
KVStore 自动本地持久化（离线可编辑）
        ↓
网络恢复 / 设备在线 → KVStore 自动跨设备同步
        ↓
dataChange 回调触发 → 应用层时间戳仲裁
        ↓
UI 刷新 + 同步面板更新
```

---

## 七、关键 API 清单（API 12）

| 能力 | API |
|------|-----|
| 创建 KVManager | `distributedKVStore.createKVManager(bundleName)` |
| 获取单版本 KV 存储 | `kvManager.getKVStore(storeId, options)` |
| 写入 | `kvStore.put(key, value)` |
| 读取 | `kvStore.get(key)` |
| 订阅变更 | `kvStore.on('dataChange', SUBSCRIBE_TYPE_ALL, cb)` |
| 查询 | `kvStore.getResultSet(batch)` / 遍历 key |
| 删除（仅用于非墓碑场景） | `kvStore.delete(key)` |

> 注意：需要在 `module.json5` 申请 `ohos.permission.DISTRIBUTED_DATASYNC` 权限，并确保多设备登录同一华为账号、开启蓝牙/Wi-Fi 组网。

---

## 八、现有代码需要调整的点

对照当前工程 `entry/src/main/ets/model/` 下的模型：

| 文件 | 现状 | 需调整 |
|------|------|--------|
| `Favorite.ets` | 只有 `heritageId` + `createdAt` | 补 `id`、`tags[]`、`updatedAt`、`deleted` |
| `Note.ets` | 只有 `heritageId` + `content` + `updatedAt` | 补 `id`、`createdAt`、`deleted` |
| （缺失） | 无 `Progress`、`SyncMeta` 模型 | 新增 `Progress.ets`、`SyncMeta.ets` |
| `City.ets` / `Heritage.ets` | 静态数据，无需同步 | 保持不变（预置数据） |

---

## 九、方案一句话总结

> **distributedKVStore 单库存储**（value 存 JSON）→ **UUID 固定 key + 墓碑删除** → **`updatedAt` 时间戳做记录级冲突仲裁**（`dataChange` 回调二次裁决）→ **`meta:syncState` 驱动同步状态面板**。
